# 格记 v0.11.0 Android 真机测试报告

## 1. 结论

- 测试日期：2026-08-11 至 2026-08-12（Asia/Shanghai）。
- 总体结论：通过，可发布到 `main` 并作为 `internal-debug` 渠道更新包。
- 自动化：Web `156/156`、Server `33/33`、Desktop `3/3`、版本元数据 `3/3` 全部通过。
- 静态检查：严格 TypeScript、Desktop JavaScript 语法、`git diff --check` 全部通过。
- 构建：Web 生产构建、Capacitor Android 同步、Android 单元测试、Debug APK、unsigned Release APK 全部成功。
- 真机：新增的速算、番茄钟、资料分析复盘、OCR、通用模式、PixelGrid 动效、局域网设备直连和既有模块均已覆盖。
- 稳定性：最终 crash buffer 为空，应用日志未发现 FATAL、AndroidRuntime 崩溃或 WebView Console Error。
- 数据：测试结束后已精确清理 QA 业务记录和队列，保留设备身份、局域网开关、电脑同步偏好和接收前恢复点。

## 2. 测试对象

| 项目 | 值 |
| --- | --- |
| 应用 | 格记 |
| 版本 | `0.11.0` |
| Android versionCode | `14` |
| 包名 | `com.wjy.kaogong` |
| 发布渠道 | `internal-debug` |
| 状态/同步/备份协议 | `3 / 3 / 1` |
| 真机 | Xiaomi M2006J10C |
| 系统 | Android 12 / API 31 |
| 屏幕 | 1080x2400，440 dpi |
| 安装方式 | `adb install -r` 原地覆盖 |
| 交付 APK | `artifacts/release-v0.11.0/kaogong-checkin-v0.11.0.apk` |
| APK 大小 | 48,517,268 字节 |
| APK SHA-256 | `C710E527A4FE3F3385FE500C99EBE1B71ADFBBBB5B16385A8CFD1E47D0DBF9C5` |
| 签名证书 SHA-256 | `5CE8F8E32FFA46997B95D35C25C1D47B7C89589CA44BA65F6680F4C09843A45F` |

本次交付 APK 与 GitHub v0.7.1 历史 APK 的签名证书完全一致，可在已安装历史版本的手机上原地升级。Gradle 同时成功生成 `app-release-unsigned.apk`，但该文件没有签名，不能用于设备更新，因此不作为交付物。

升级前已创建完整私有数据备份：

- `artifacts/device-backups/20260811T194102+0800-pre-v0.11.0/app-data.tar`
- SHA-256：`332149CC2724C3749C96051B14AD541149ECD5FB07D2FD0936DA96BEC5334DB7`

## 3. 自动化与构建

| 检查 | 结果 |
| --- | --- |
| `cd web && npm test` | 156 通过，0 失败 |
| `cd web && npx tsc --noEmit` | 通过 |
| `cd web && npm run build` | 通过，279 个模块 |
| `cd server && npm test` | 33 通过，0 失败 |
| `cd desktop && npm test` | 3 通过，0 失败 |
| `cd desktop && npm run check` | 通过 |
| `node --test scripts/test/*.test.mjs` | 3 通过，0 失败 |
| `npx cap sync android` | 通过，3 个 Capacitor 插件同步 |
| `gradlew testDebugUnitTest assembleDebug assembleRelease` | 通过，443 个任务 |
| APK 身份、签名和内嵌门禁 | 通过 |
| `git diff --check` | 通过，仅有 Windows LF/CRLF 提示 |

Web 测试期间出现固定测试端口 `24678` 已占用和 Node 环境不实现 Android UDP 插件的诊断输出；相关断言全部通过。真实 UDP/NSD、配对码和 TCP 快照传输另在 Android 插件及真机上验证。

## 4. 真机测试矩阵

### 4.1 安装、启动与持久化

- 使用 `adb install -r` 覆盖安装成功，没有卸载、清数据或重启手机。
- UID 始终为 `10260`，首次安装时间始终为 `2026-07-30 17:21:32`。
- 覆盖安装前后的业务记录、设备 ID 和偏好均可读取。
- 最终包冷启动两次分别为 `1149 ms` 和清理后 `954 ms`。
- 首屏内容和导航整体出现；右下角新增按钮没有早于页面主体单独闪现。
- 横屏 CSS 视口为 873x342，竖屏 CSS 视口为 393x822；两种方向均无页面级横向溢出或控件重叠。
- 自动旋转在测试结束后恢复为开启，用户旋转基准恢复为 0。

### 4.2 模式、任务与 PixelGrid

- 考公/通用模式双向切换、文案、底部导航和统计口径正确，数据不随模式切换丢失。
- 通用模式的数量型任务完成 `0/3 -> 1/3 -> 2/3 -> 1/3 -> 3/3`，增减边界正确。
- 新建、管理、排序、归档、恢复和删除任务入口均可点按。
- 删除任务使用应用内确认弹窗，不再调用原始 Android/浏览器确认框。
- 打卡成功、答对、答错、全部完成、清除任务和切换模式均显示语义匹配的 PixelGrid 动效。
- 全部完成庆祝层显示后自动结束，不阻挡后续操作。
- 启用“减少动画”时 PixelGrid 使用静态完成帧，内容与状态反馈仍可理解。

### 4.3 秒表、倒计时和番茄钟

- 秒表：开始、打点、暂停、继续、结束、保存、历史详情和数据对比通过。
- 倒计时：自定义 2 秒、自动结束、完成反馈、保存和历史标签通过。
- 番茄钟配置边界：`0/99/-5/1` 分别夹取为 `1/60/1/2`。
- 番茄钟实际运行完 1 分钟专注，自动生成 `pomodoro` 计时记录。
- 专注、短休息、长休息、四轮周期、跳过阶段、暂停和继续通过。
- WebView 重载恢复、Home 后台后通过 ADB 返回、按绝对时间自动完成通过。
- 结束番茄的取消/确认、历史标签、删除及应用内确认弹窗通过。
- 清理后 `kgc-pomodoro-v1` 不存在，无运行中或暂停中的番茄状态。

### 4.4 百化分、公式与速算

- 百化分 24 条、公式 36 条及 8 个公式分类完整显示。
- 百化分和公式的完整/随机练习、翻面、自评、判分、总结和历史流程通过。
- 速算 30 类均可选择，简单/一般/困难三档和 5/10/15/20/25 题预设完整。
- 整数、小数、选择题、正确答案、错误答案、下一题、总结、错题和历史通过。
- 正确答案显示 PixelGrid 扩散反馈，错误答案显示收束反馈。
- 错题与历史清空使用应用内确认弹窗，并以软删除同步到 v3 状态。
- 自动化逐类逐难度生成并判定所有速算题，容差只接受合理范围，不接受相邻错误答案。

### 4.5 资料分析讲解、相机与 OCR

- 手动输入题目、用户答案、正确答案、生成讲解、保存历史和删除通过。
- 输出固定包含题型识别、找数检查、核心关系、最短路径、估算策略、易错复盘六段。
- `2025 年现期，同比增长，问 2024 年` 正确识别为基期量题。
- 真实 ADB 触控“拍照识题”成功唤起 `com.android.camera/.OneShotCamera`，返回应用正常；为避免留下系统相册文件，本轮未按快门。
- 相册真实图片选择、压缩、预览、本地 ML Kit 中文 OCR 和文本回填通过。
- 四张用户参考图 OCR 结果：

| 图片 | 文本块 | 字符 | 耗时 |
| --- | ---: | ---: | ---: |
| `69401d..._720.jpg` | 21 | 103 | 1527 ms |
| `9cd22b..._720.jpg` | 22 | 136 | 837 ms |
| `e4296b..._720.jpg` | 32 | 189 | 871 ms |
| `9ed6e7..._720.jpg` | 18 | 91 | 782 ms |

- OCR 和讲题都在本机执行；同步记录只保存文本和结构化复盘，不保存原始图片。

### 4.6 统计、历史与导航

- 考公摘要和通用摘要的连续天数、完成率、活跃天数及计划结束口径正确。
- 月历、14 天趋势、计时聚合、最近记录日和空状态均可用。
- 从月历真实点击进入 `/stats/day/2026-08-12`，日期详情、前后日期和返回概览通过。
- 计时历史的秒表、倒计时、番茄标签及删除入口正确。
- 四个根页面、任务管理、计时历史、统计和日期详情均完成竖屏控件遍历。
- Android 返回键优先关闭最高层弹窗；二级页返回父页，根页返回最小化。

### 4.7 局域网设备直连

- “允许附近设备发现本机”和“主动搜索附近设备”为两个独立开关。
- 发现信息显示设备名、局域网地址、端口、六位配对码和二维码。
- 手动地址/配对码、选择设备、发送本机记录、接收对方记录和每次目标确认入口通过。
- 目标手机收到“允许发送”和“允许接收”应用内确认；取消和确认分支均覆盖。
- 手机到模拟器、模拟器原样回传到手机的真实往返通过，手机最终返回 `committed`。
- 往返快照：7,677 字节，SHA-256 `99e777f79d36fcf98b31349d767d8a6bc753137a47b8fc967c9de9eb0048f6b9`。
- 快照包含 3 条番茄、7 条速算和 2 条复盘；ID、软删除状态和 v3 集合完整保留。
- 接收前恢复点创建和最近 5 份保留策略通过；恢复入口可见。
- 修复接收摘要遗漏：训练数现在合计背诵、速算和资料复盘，软删除记录不计入摘要。
- v0.10 旧 v3 快照缺少新增训练集合时按空数组处理，首次直连不会因摘要统计失败。
- 修复 v3 校验遗漏：速算和资料复盘集合内重复 ID 会被拒绝。

### 4.8 主题、动效和控件布局

- 通过 ADB 切换系统深色/浅色，应用主题响应正常，测试结束恢复浅色基准。
- `prefers-reduced-motion` 媒体查询能禁用非必要连续动效。
- SwiftPixelGrid 风格语义动效在打卡、答题、任务完成和清除动作中保持一致。
- 所有主要按钮和输入控件的竖屏点击区域约为 42 至 48 CSS px；计时主按钮为 96x96 CSS px。
- 竖屏和横屏均未观察到文字截断、卡片嵌套异常、悬浮按钮遮挡或导航重叠。

## 5. 性能与稳定性

连续执行 8 轮四页切换和双向长列表滚动后的 Android `gfxinfo`：

| 指标 | 结果 |
| --- | ---: |
| 总帧数 | 5,969 |
| Janky frames（新标准） | 152（2.55%） |
| P50 / P90 / P95 / P99 | 16 / 20 / 21 / 31 ms |
| Missed Vsync | 45 |
| Slow UI thread | 82 |
| Slow bitmap uploads | 0 |
| TOTAL PSS | 221,479 KB |
| TOTAL RSS | 338,444 KB |
| Graphics | 76,580 KB |

该采样包含快速连续切页、PixelGrid/页面动效和长列表滚动，未观察到黑屏、卡死、触控失效或动画结束后残留。2.55% 是保留的真机压力基线，不宣称零卡顿。

## 6. 测试中发现并修复的问题

| 编号 | 问题 | 修复与回归 |
| --- | --- | --- |
| BUG-01 | 同比题询问上一年时偶尔被误识别为现期量 | 年份关系优先，回归测试确认识别为基期量 |
| BUG-02 | 番茄阶段时长函数在严格 TypeScript 下存在隐式 `any` | 补齐类型并增加配置边界测试 |
| BUG-03 | 局域网接收确认只统计旧背诵记录，速算和复盘会显示为 0 条训练 | 摘要合计三类训练并增加传输回归测试 |
| BUG-04 | v3 校验没有拒绝重复的速算/复盘 ID | 增加集合内唯一性检查和回归测试 |
| BUG-05 | 首屏右下角新增按钮早于主体加载 | FAB 与启动 shell 共用阶段，冷启动截图验证整体出现 |

## 7. 限制与说明

- Android 相机入口和返回路径已通过；为了不在用户系统相册中留下测试照片，最终回归没有按下快门。完整图片识别链路使用相册中的四张真实图片验证。
- `assembleRelease` 生成的是 unsigned APK。交付包使用与历史版本一致的 Android Debug 证书签名，符合当前 `internal-debug` 渠道。
- Web 自动化中的 Node UDP 插件缺失只影响 Node 测试环境探针，不影响 Android 原生插件；Android 真机发现、配对和往返传输已通过。
- MIUI 的 `uiautomator dump` 会打印缺少 `theme_compatibility.xml` 的系统主题兼容警告；UI 树仍成功生成，该警告不来自应用。

## 8. 最终设备状态与证据

最终冷启动后验证：

- 旧存储：任务、子任务、打卡、计时、百化分、公式、速算、复盘均为 0。
- v3：任务、打卡、计时、背诵、速算、复盘均为 0。
- 同步队列和 v3 outbox 均为 0，番茄状态不存在。
- 模式为考公；设备 ID `084994e0-cd80-4167-a5b8-d50d0aa42b58` 保留。
- 局域网发现与主动搜索开关保留开启，电脑同步偏好保留关闭。
- 版本为 `0.11.0 (14)`，UID 与首次安装时间未变化。

本机证据：

- `artifacts/qa-v0.11.0/16-final-cold-launch.png`
- `artifacts/qa-v0.11.0/17-final-portrait.png`
- `artifacts/qa-v0.11.0/18-final-clean-state.png`
- `artifacts/release-v0.11.0/release-manifest.json`
- `artifacts/release-v0.11.0/kaogong-checkin-v0.11.0.apk`
