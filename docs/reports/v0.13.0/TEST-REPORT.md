# 格记 v0.13.0 Android 真机测试报告

## 1. 验收结论

- 测试日期：2026-08-13（Asia/Shanghai）。
- 总体结论：通过，可作为 `internal-debug` 渠道更新包发布。
- 本轮修复了旧数据恢复失败、设备发送被误判为拒绝、后台短暂停留导致接收服务关闭、并发确认弹窗重叠等问题。
- 本轮还选择性融合了 `F:\Claude\GongkaoCheckin` 的学习驾驶舱、能力画像、连续底栏指示和克制动效，并增加 72 道题库的专注答题链路。
- 自动化：Web `172/172`、Server `33/33`、Desktop `3/3`、发布脚本 `8/8` 全部通过。
- 静态检查与构建：TypeScript、Vite 生产构建、Android `testDebugUnitTest assembleDebug`、版本一致性和 `git diff --check` 全部通过。
- 真机：Xiaomi M2007J1SC，Android 13，使用 ADB/CDP；未卸载、未清除应用数据、未重启手机。
- 局域网同步使用一台 Android 真机加 ADB TCP 协议模拟完成协议回归。因本轮只有一台 Android 设备在线，未把“双 Android 实体互搜和双向完整替换”记为已通过。

## 2. 测试对象与发布包

| 项目 | 值 |
| --- | --- |
| 应用 | 格记 |
| 版本 | `0.13.0` |
| Android versionCode | `16` |
| 包名 | `com.wjy.kaogong` |
| 发布渠道 | `internal-debug` |
| 状态/同步/备份协议 | `3 / 3 / 1` |
| 真机 | Xiaomi M2007J1SC |
| 系统 | Android 13 |
| 屏幕 | 1080x2340，440 dpi |
| 安装方式 | `adb install -r` 原地覆盖 |
| 首次安装时间 | `2026-08-05 23:55:21`，升级后保持不变 |
| 最后更新时间 | `2026-08-13 15:22:53` |
| 交付 APK | `release/kaogong-checkin-v0.13.0.apk` |
| APK 大小 | `48,759,395` 字节 |
| APK SHA-256 | `058B5B9107C331D740FB644F36638FA6384468B93CCAB3D3CE811405B85A196B` |

发布包与真机验收包为同一字节流。正式 JKS 证书与手机现有历史 Debug 证书不同，因此继续使用历史 `internal-debug` 签名，确保现有安装可原地升级并保留本地记录。

## 3. 自动化、静态检查与构建

| 检查 | 结果 |
| --- | --- |
| `cd web && npm test` | `172` 通过，0 失败 |
| `cd server && npm test` | `33` 通过，0 失败 |
| `cd desktop && npm test` | `3` 通过，0 失败 |
| `node --test scripts/test/*.test.*` | `8` 通过，0 失败 |
| `cd web && npx tsc --noEmit` | 通过 |
| `cd web && npm run build` | 通过，299 个模块 |
| `cd desktop && npm run check` | 通过 |
| `cd web/android && ./gradlew testDebugUnitTest assembleDebug` | 通过 |
| `node scripts/verify-version.mjs` | `0.13.0 (16)` 及协议元数据一致 |
| `git diff --check` | 通过 |
| APK 清单、大小与 SHA-256 | 一致 |

非阻断诊断：Web 测试并行运行时偶尔提示 WebSocket 端口 `24678` 已占用，但全部断言通过；Node 环境不实现 Android `UdpSocket` 插件，原生入口由真机回归覆盖；Vite 主 chunk 约 561 KB，属于后续性能拆包建议，不阻断本版本发布。

## 4. 数据恢复回归

### 4.1 用户路径与失败提示

| 用例 | 预期与结果 |
| --- | --- |
| 首屏进入只读保护 | 显示“本地数据等待恢复”，明确说明原始数据未被覆盖，结果通过 |
| 打开恢复中心 | 提供“设置 → 本地数据等待恢复 → 验证并恢复”的明确路径，结果通过 |
| 恢复失败 | 显示错误码、禁止卸载/清数据提示及另一台设备接收路径，结果通过 |
| 恢复成功 | 解除只读状态，显示已修复历史关联数量，结果通过 |
| 恢复后任务操作 | 新增、打卡、撤销、编辑和删除均可正常写入，结果通过 |

### 4.2 孤儿历史记录修复

通过隔离夹具复现“历史记录引用已不存在任务”的旧数据：

1. 保留迁移前原始备份，不覆盖来源字节。
2. 对仍有效的孤儿进度和计时创建归档项目“已恢复的历史项目”。
3. 已删除墓碑不复活，无法解释的损坏关联继续失败关闭。
4. 重建后执行完整 v3 校验，再一次性提交主状态。

结果：自动化夹具通过。截图中原问题所在的另一台设备本轮没有重新连接，因此这里只声明“故障夹具与当前真机恢复链路通过”，不声明该截图设备已再次实测。

## 5. 局域网设备同步回归

### 5.1 真机入口

- “允许附近设备发现本机”和“主动搜索附近设备”两个开关均可独立开启、关闭。
- 发现页可显示局域网地址、配对码和二维码，手动地址、配对码、二维码入口可操作。
- 发送和接收保持为不同动作；完整替换前显示来源、记录摘要和风险说明。
- 应用短暂进入后台后再回到前台，已显式开启的接收服务仍保持；主动搜索按隐私边界暂停。
- 测试结束时发现和搜索均已关闭。

### 5.2 ADB TCP 协议模拟

| 用例 | 结果 |
| --- | --- |
| 错误配对码 | 返回 `PAIRING_INVALID`，通过 |
| 目标设备点“拒绝” | 返回 `REJECTED`，通过 |
| Android 返回键/遮罩 | 不会把显式确认转换成拒绝，通过 |
| 允许发送 | 快照摘要、UTF-8 字节数、SHA-256、schema 校验通过 |
| 接收同一快照 | 返回 `ready → committed`，通过 |
| 接收前恢复点 | 自动创建，失败时不修改主状态，通过 |
| 接收后电脑同步 | 自动暂停，避免旧电脑快照回写，通过 |
| 两个并发请求 | 始终只有一个可交互弹窗，上一张离场后才展示下一张，通过 |

当前测试边界：只有一台 Android 真机在线，第二端由 TCP 协议夹具模拟。因此 NSD 双设备实体互搜、手机到平板和反向完整替换仍需在两台 Android 同时在线时补做一次验收。

## 6. 学习驾驶舱与 Opus5 融合

本轮查看并对照了 `F:\Claude\GongkaoCheckin`，选择融入适合现有产品结构的部分，没有整体换皮。

| 模块 | 已选方案 | 真机结果 |
| --- | --- | --- |
| 今日首页 | 今日事项、专注分钟、待复盘、建议可用时间组成学习驾驶舱 | 通过 |
| 智能下一步 | 逾期优先、未完成任务、复盘和空计划依次给出可执行动作 | 通过 |
| 双轨计划 | 考公模式为行测/申论，通用模式为行动/习惯 | 通过 |
| 能力画像 | 基于本地打卡、计时、速算、复盘生成，不调用云端 | 通过 |
| 底部导航 | 连续滑动指示条跟随页面手势进度 | 通过 |
| 页面动效 | 使用克制入场、状态反馈和 PixelGrid 语义动效 | 通过 |
| 未采用 | 整体 Material 3 换皮、蓝紫渐变、彩色侧条、玻璃庆祝层、强制触觉 | 不适用 |

冷启动 120ms、620ms、1520ms 截图显示主内容、底栏和右下角新增按钮属于同一启动阶段，未复现 FAB 先于其他内容单独出现。

## 7. 全模块真机回归

### 7.1 启动、导航与任务

- 今日、计时、背诵、设置四个根页和各二级页可进入并返回。
- 四根页左右滑动、底部点按和连续指示条状态一致。
- 任务新增、数量调整、打卡、撤销、编辑、归档、恢复和删除通过。
- 删除确认使用应用内弹窗，危险按钮白字红底，点按后记录实际删除。
- 考公/通用模式双向切换通过，业务记录不因模式切换丢失。

### 7.2 计时与自律工具

- 秒表开始、打点、暂停、继续、保存、历史查看和删除通过。
- 倒计时使用滚轮设置，不唤起数字键盘；开始、暂停、继续、结束确认通过。
- 番茄钟四组滚轮、专注、短休息、长休息、暂停、继续和结束确认通过。
- 测试结束后番茄钟设置恢复为 `27/7/17/6`。

### 7.3 背诵、速算、题库与 Skills

- 百化分、公式和速算分类、难度、题量、答题、反馈、总结与历史通过。
- 题库包含 72 道“陪陪刷”资料分析题，搜索、分类筛选和选择题目通过。
- 专注答题支持单题提交、答案反馈，并可把原题与作答带入 Skills 复盘。
- Skills 使用可扩展注册表，当前启用“陈怀安资料分析”，输出题型识别、找数检查、核心关系、最短路径、估算策略、易错复盘六段讲解。
- 相机、相册入口及取消路径通过；本版本不将本地 OCR 识别能力写成已实现。

### 7.4 设置、统计与外观

- 浅色/深色、启动动画、计划日期、更新检查和版本信息通过。
- 热力图、连续记录、计时统计、能力画像和日期详情通过。
- 应用内确认弹窗的焦点、返回键、遮罩、队列和危险按钮状态通过。
- 未发现应用崩溃日志或 WebView 运行时致命错误。

## 8. 性能、数据保护与清理状态

真机帧统计：

| 指标 | 结果 |
| --- | ---: |
| 50th percentile | 12 ms |
| 90th percentile | 15 ms |
| 95th percentile | 18 ms |
| 99th percentile | 26 ms |
| Janky frames | 143 / 2242，6.38% |

数据保护：

- `firstInstallTime` 保持为 `2026-08-05 23:55:21`，证明未卸载重装。
- 安装前后 WebView Local Storage LevelDB 文件 SHA-256 一致。
- 未执行清除应用数据、卸载或设备重启。
- 完整接收前创建恢复点；接收后暂停电脑同步，避免旧电脑状态覆盖新设备记录。

测试结束状态：考公模式、浅色、启动动画开启；发现和搜索关闭；没有本轮临时任务、计时、百化分、公式或速算记录；原有 2 条有效复盘保留；协议测试恢复点已删除。电脑同步因安全策略保持关闭。

## 9. 证据与残余验证

真机截图、UI XML、协议请求/响应和性能输出保存在本地 `.artifacts/v0.13.0-device/` 及 `.artifacts/v0.13.0-*`。这些文件可能包含设备状态和测试快照，仅用于本机审计，不提交到 GitHub。

| 类型 | 已完成 | 仍需补做 |
| --- | --- | --- |
| 单 Android 真机 | 全模块点按、恢复入口、发现入口、后台恢复、视觉和性能 | 无阻断项 |
| 自动化/夹具 | 恢复故障、完整替换、队列、领域校验、构建和发布清单 | 无阻断项 |
| ADB TCP 第二设备模拟 | 配对、拒绝、允许、发送、接收、恢复点、并发请求 | 无阻断项 |
| 双 Android 实体 | 本轮未具备两台同时在线条件 | NSD 互搜及双向完整替换 |
| 截图中的旧设备 | 故障夹具已复现并修复 | 该设备重新连接后的现场恢复 |

本报告只记录实际执行的测试，不将未执行的双 Android 实体互传写成已通过。
