# 双应用离线二维码测试（已确认）

保留原格记及存档，安装独立包名的格记测试 A、B。各包使用各自本地数据及心愿签名身份，不复制原身份或钱包。

通过 Gradle qrTestSlot=A/B 显式构建。图片识别仅在这些包的 debug 构建开启；普通包和 release 原生入口拒绝调用。界面标识测试用途。

使用 Android 本地文件选择器读取截图，由 ZXing 解码真实像素，再交给现有心愿验签、收券、请求、签发、扣星流程。禁止注入审批、绕过签名或联网传送。限制文件大小与解码像素，后台解码，不申请全盘读取权限。

验收：A 心愿截图 → B 收券；B 请求截图 → A 签发；A 钥匙截图 → B 确认扣星。检查重复钥匙、错误内容、余额不足、取消和重启。保留证据、恢复网络，不修改原存档。

图片识别链路与两台手机相机光学扫描分开报告；此次不宣称后者通过。
