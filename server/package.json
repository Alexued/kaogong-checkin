{
  "name": "kaogong-checkin-server",
  "private": true,
  "version": "0.10.0",
  "description": "考公打卡 本地服务器（REST + WebSocket + UDP 广播 + 静态托管）",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
